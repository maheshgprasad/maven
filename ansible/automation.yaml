---
- name: Application deployment automation
  hosts: localhost
  become: no
  become_user: mahesh
  gather_facts: false
  tasks:
    - name: "Package spring boot application"
      shell: cd .. && mvn clean package 
      register:  mvn_pkg_out
    - debug:
        var: mvn_pkg_out.stdout_lines
